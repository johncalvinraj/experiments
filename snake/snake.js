var canvas = document.getElementById('canvas');
var ctx = canvas.getContext('2d');
var raf;
var posX = 500, posY = 500
var deltaX = 1, deltaY = 0
var fps = 60

function draw() {
                            
  var newX = posX + deltaX
  var newY = posY + deltaY
  console.log(newX,newY)
  ctx.beginPath();
  ctx.moveTo(posX, posY);
  ctx.lineTo(newX, newY);
  ctx.stroke();
  posX = newX
  posY = newY
  setTimeout(function() {
    requestAnimationFrame(draw);
  }, 1000 / fps);
}

document.addEventListener('keydown', event => {

  //movement for player 1
  /*if (event.code === 'KeyA') {
    if (deltaX === 1) {
      deltaX = 0
      deltaY = 1
    }
    if (deltaX === 0 && deltaY === 1) {
      deltaX = -1
      deltaY = 0
    }
    if (deltaX === -1) {
      deltaX = 1
      deltaY = 0
    }
    draw()
  }

  if (event.code === 'KeyD') {
    if (deltaY === 1) {
      deltaY = 0
    }
    if (deltaY === 0 && deltaX === 1) {
      deltaY = -1
    }
    if (deltaY === -1) {
      deltaY = 0
      deltaX = -1
    }
    if (deltaX === -1 && deltaY === 0) {
      deltaY = 1
    }
    draw()
  }*/

  //movement for player 2
  if (event.code === 'ArrowLeft') {
    deltaX = -1
    deltaY = 0
  }

  if (event.code === 'ArrowRight') {
    deltaX = 1
    deltaY = 0   
  }

  if (event.code === 'ArrowUp') {
    deltaX = 0
    deltaY = -1
  }  

  if (event.code === 'ArrowDown') {
    deltaX = 0
    deltaY = 1
  }    
  
  //draw()
})  

draw()



























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































